{
  "name": "@larsthorup/react-native-mocha",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "eslint": "eslint . --fix --max-warnings 0 --ignore-path .gitignore",
    "pack": "rm -rf *.tgz && npm pack && mv *.tgz larsthorup-react-native-mocha-local.tgz",
    "test": "npm run eslint"
  },
  "devDependencies": {
    "eslint": "^7.27.0"
  }
}
